<h4 class="mb-4">マッチング</h4>

<% if (!registered) { %>
    <div class="alert alert-danger" role="alert">
        右上のアイコンから性別、Skype IDを登録してください
    </div>
<% } %>

<div class="ml-4">
    <h6>参加条件</h6>
    <div class="ml-4">
        <p>
        <%- include("./module/ok_ng", {"bool":registered}) %>
        性別、Skype IDが登録済み
        </p>

        <p>
        <%- include("./module/ok_ng", {"bool":twitter_safe}) %>
        Twitterの登録日が半年以上前
        </p>
    </div>
</div>

<% let disabled = isReady ? "" : "disabled" %>
<div class="mt-5">
    <div class="text-center">
    <form class="d-inline" method="POST" action="/entry/">
        <input type="hidden" name="entry_type" value="act2">
        <button type="submit" class="btn bg_red" <%= disabled %>>サシ劇</button>
    </form>
    <form class="d-inline ml-2" method="POST" action="/entry/">
        <input type="hidden" name="entry_type" value="act3_7">
        <button type="submit" class="btn bg_red" <%= disabled %>>複数人劇</button>
    </form>
    </div>
</div>

<p class="mt-2 text-center text-secondary">※マッチングは30分間有効です。</p>
<%- include("./module/entry_explain", {}) %>

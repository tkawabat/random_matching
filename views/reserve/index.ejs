<h4 class="mb-4">募集劇</h4>

<% let disabled = user ? "" : " disabled" %>
<div class="mb-4 text-center">
    <a href="/reserve/create"><button type="button" class="btn bg_red" <%=disabled%>>新しい劇を作成</button></a>
</div>


<h5>最新一覧</h5>

<div>

<% for (let r of reserveList) { %>
    <%- include("../module/reserve", {reserve:r}) %>
<% } %>

</div>
